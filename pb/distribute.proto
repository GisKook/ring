syntax="proto3";
package Carrier;

import "base.proto";
import "distribute_logrt.proto";

message Distribute{ 
	Header header = 1;
	enum Protocol{
		UNKNOWN = 0;
		LOGRT = 1;
	}
	Protocol protocol = 2;
	DistributeLogRt logrt = 3;
}
